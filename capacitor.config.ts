import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_arquitectura',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
